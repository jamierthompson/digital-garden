// Placeholder embed slot — resolves to the shared Color Engine placeholder. The `embedKey`
// stays registered (`src/lib/keys.ts`) so a published `liveEmbed` block still resolves while
// the Color Engine is rebuilt on the new foundation.
export { default } from "../ColorEngineStub";
